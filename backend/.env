# Configuración del servidor
PORT=3001

# Base de datos MySQL (XAMPP por defecto)
DB_HOST=localhost
DB_PORT=3306
DB_USER=un_usr
DB_PASS=una_clave
DB_NAME=secondhand_db

# JWT — cámbialo por una cadena secreta larga
JWT_SECRET=secondhand_super_secreto_2026_UIS

# Tiempo de expiración del token (ej: 7d, 24h, 30m)
JWT_EXPIRES=7d
